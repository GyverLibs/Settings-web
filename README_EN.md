This is an automatic translation and may be incorrect in some places. See the source README and examples for authoritative information.

# Settings web
Library web application[Settings](https://github.com/GyverLibs/Settings)

## How to collect
- Install[VS Code](https://code.visualstudio.com/download)
- Install[Node JS](https://nodejs.org/en/download/prebuilt-installer)
- Open a folder in VS Code
- Console **Ctrl + `**
- `npm install`Wait for the dependency setup
- `npm run build`
- The project will assemble in the dist folder

## Development and debugging
Specify the local IP board in the file`webpack.config.js`. Console`npm run dev`will launch the dev server and open the browser, will update the site when changes in the code. To close,`Ctrl+C`console

## How to use
- Caching:`index`no cache,`js`and`css`cache
- Specify gzip header
